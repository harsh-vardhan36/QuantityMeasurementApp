app.repository.type=database

# Production Database (MySQL)
db.url=jdbc:mysql://localhost:3306/quantity_measurement
db.username=root
db.password=root123
db.driverClassName=com.mysql.cj.jdbc.Driver

# Connection Pool
db.pool.maxSize=10
db.pool.minIdle=2
db.pool.connectionTimeoutMs=30000
db.pool.idleTimeoutMs=600000
db.pool.maxLifetimeMs=1800000

# Test Database (H2)
test.db.url=jdbc:h2:mem:quantity_measurement_test;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false
test.db.username=sa
test.db.password=
test.db.driverClassName=org.h2.Driver

logging.level=INFO